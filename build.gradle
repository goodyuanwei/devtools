apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    implementation gradleApi()
    implementation localGroovy()
    implementation 'com.android.tools.build:gradle:4.1.2'
    implementation group: 'org.javassist', name: 'javassist', version: '3.22.0-GA'
}

repositories {
    mavenCentral()
}


uploadArchives {
    repositories {
        mavenDeployer {
            pom.groupId = 'io.github.goodyuanwei'
            pom.artifactId = 'devtools'
            pom.version = '1.0.0'

            repository(url: "https://s01.oss.sonatype.org/content/repositories/snapshots/") {
                authentication(userName: "yuanweibetter", password: "Douhong@20202")
            }

            //本地的Maven地址设置为D:/repos
            // repository(url: uri('/Users/yuanwei/Desktop/repos'))
        }
    }
}